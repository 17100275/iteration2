<h1>All Records:</h1>

<% if current_user!=nil %>
  <% if "haamid@gmail.com" != current_user.email %>
    <b> <%= link_to 'New Record', new_record_path %> </b>
  <% end %>
<% end %>

<% if current_user!=nil %>
  <% if "haamid@gmail.com" == current_user.email %>
    <p><b> <%= link_to 'Search Records', :controller => :records, :action => :search_get %> </b></p>
  <% end %>
<% end %>

<table>
  <tr>
    <th>Prescriptions</th>
    <th>Symptoms</th>
    <th>Date</th>
    <th>User</th>
    <th>                                    </th>
    <th>                                    </th>
    <th>                                    </th>

    <th>Actions</th>
    
  </tr>
 
  <% @records.each do |record| %>
    <tr>
      <% if current_user!=nil %>
        <% if "haamid@gmail.com" == current_user.email %>
          <td><%= record.Prescriptions %></td>
          <td><%= record.Symptoms %></td>
          <td><%= record.Date %></td>
          <td><%= record.user.email %></td>
          <td>                                    </td>
          <td>                                    </td>
          <td>                                    </td>

          <td> <b> <%= link_to 'Show', record_path(record) %> </b> </td>
          <td> <b> <%= link_to 'Edit', edit_record_path(record) %> </b> </td>
        <% else %>
          <% if record.user == current_user %>
            <td><%= record.Prescriptions %></td>
            <td><%= record.Symptoms %></td>
            <td><%= record.Date %></td>
            <td><%= record.user.email %></td>
          
            <th>         </th><th>         </th><th>         </th><th>         </th><th>         </th><th>         </th>
            <td> <b> <%= link_to 'Show', record_path(record) %> </b> </td>
            <td> <b> <%= link_to 'Edit', edit_record_path(record) %> </b> </td>
            <td> <b> <%= link_to 'Delete', record_path(record), method: :delete, data: { confirm: 'Are you sure you want to delete this record?' } %> </b> </td>
          <% end %>
        <% end %>
      <% end %>
    </tr>
  <% end %>
</table>

<p><b> <%= link_to 'Back to Welcome Page', :controller => :welcome, :action => :index %> </b></p>